# Before execution the TRANSMUT folder and reports should not exist
> checkBeforeTransmut

# Execute TRANSMUT-Spark plugin
# Should fail because the file transmut.conf does not exist
-> transmut

# If transmutation plugin fails, the TRANSMUT folder and reports should not exist
> checkBeforeTransmut