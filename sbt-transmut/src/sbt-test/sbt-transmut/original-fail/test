# Before execution the TRANSMUT folder and reports should not exist
> checkBeforeTransmut

# Execute TRANSMUT-Spark plugin
# Should fail because the original program fails in the tests
-> transmut

# If transmutation plugin fails because the original program fails, the TRANSMUT folder and mutated-src should be deleted
> checkBeforeTransmut